import { LayerLoaderDirective } from './layer-loader.directive';

describe('LayerLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new LayerLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
